# Core scientific stack — pinned to the ABI the system torch/pandas/scipy were built against.
numpy==1.26.4
pandas>=2.1,<2.3
scipy>=1.11,<1.13
matplotlib>=3.6,<3.9
pyyaml>=6.0

# Imaging / IO
opencv-python-headless==4.10.0.84   # 4.x supports numpy<2 (5.x forces numpy>=2)
pydicom>=3.0
python-gdcm>=3.0.24                  # JPEG-LS decoder for the compressed cines (numpy-ABI-agnostic)
shapely>=2.0
openpyxl>=3.1

# Notebooks
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.10
nbconvert>=7.16

# Deep learning (torch 2.7 + CUDA come from system site-packages; not re-pinned here)
ultralytics>=8.3          # YOLO11-seg baseline
# NOTE: pip-installing ultralytics/opencv can pull numpy>=2 and break the system torch/pandas ABI.
# After any such install, re-pin:  pip install "numpy==1.26.4" "opencv-python-headless==4.10.0.84"
# transformers>=4.44      # added in Phase 5 (RT-DETR / Mask2Former comparison)
